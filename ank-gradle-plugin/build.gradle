apply plugin: 'java-gradle-plugin'

group = 'io.kategory'
version = '0.1.1'

dependencies {
    compile project(':ank-core')
    testCompile 'junit:junit:4.12'
}

gradlePlugin {
    plugins {
        ank {
            id = 'ank-gradle-plugin'
            implementationClass = 'io.kategory.ank.AnkPlugin'
        }
    }
}

apply from: '../gradle-mvn-push.gradle'